<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="MediaPlayer.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="using:MediaPlayer.Converters"
    xmlns:controls="using:MediaPlayer.Controls"
    RequestedTheme="Dark">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
            <conv:InvertedBoolToVisibilityConverter x:Key="BoolToVisInvert"/>

            <SolidColorBrush x:Key="AccentBrush" Color="#818CF8"/>
            <SolidColorBrush x:Key="PanelBorderBrush" Color="#28FFFFFF"/>
            <SolidColorBrush x:Key="VideoBackgroundBrush" Color="#CC000000"/>
            <SolidColorBrush x:Key="SubtleTextBrush" Color="#D9FFFFFF"/>
            <SolidColorBrush x:Key="MutedTextBrush" Color="#99FFFFFF"/>

            <SolidColorBrush x:Key="SliderTrackBrush" Color="#E6FFFFFF"/>
            <SolidColorBrush x:Key="SliderFillBrush" Color="#FF818CF8"/>

            <x:Double x:Key="ThumbSize">10</x:Double>

            <ControlTemplate x:Key="FlatSliderTemplate" TargetType="Slider">
                <Grid x:Name="LayoutRoot" Margin="{TemplateBinding Padding}">
                    <Grid x:Name="HorizontalTemplate" MinHeight="{TemplateBinding MinHeight}" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Rectangle x:Name="HorizontalTrackRect"
                                   Grid.ColumnSpan="3"
                                   Height="3"
                                   RadiusX="1.5"
                                   RadiusY="1.5"
                                   Fill="{TemplateBinding Background}"
                                   VerticalAlignment="Center"/>
                        <Rectangle x:Name="HorizontalDecreaseRect"
                                   Height="3"
                                   RadiusX="1.5"
                                   RadiusY="1.5"
                                   Fill="{TemplateBinding Foreground}"
                                   VerticalAlignment="Center"/>
                        <Thumb x:Name="HorizontalThumb"
                               Grid.Column="1"
                               Style="{StaticResource VolumeSliderThumbStyle}"
                               VerticalAlignment="Center"/>
                    </Grid>
                    <Grid x:Name="VerticalTemplate" Visibility="Collapsed"/>
                </Grid>
            </ControlTemplate>

            <LinearGradientBrush x:Key="AppGradientBrush" StartPoint="0,0" EndPoint="1,1" MappingMode="RelativeToBoundingBox">
                <GradientStop Color="#FF0A0E1C" Offset="0"/>
                <GradientStop Color="#FF0F1226" Offset="0.22"/>
                <GradientStop Color="#FF12152F" Offset="0.48"/>
                <GradientStop Color="#FF141735" Offset="0.72"/>
                <GradientStop Color="#FF16123A" Offset="1"/>
            </LinearGradientBrush>

            <ControlTemplate x:Key="RoundThumbTemplate" TargetType="Thumb">
                <Ellipse Width="10"
                         Height="10"
                         Fill="{StaticResource AccentBrush}"
                         Stroke="#66000000"
                         StrokeThickness="1"/>
            </ControlTemplate>

            <Style x:Key="VolumeSliderThumbStyle" TargetType="Thumb">
                <Setter Property="Width" Value="10"/>
                <Setter Property="Height" Value="10"/>
                <Setter Property="IsTabStop" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <Ellipse Width="10"
                                     Height="10"
                                     Fill="{StaticResource AccentBrush}"
                                     Stroke="#55000000"
                                     StrokeThickness="1"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="VolumeSliderStyle" TargetType="Slider">
                <Setter Property="Height" Value="24"/>
                <Setter Property="MinHeight" Value="24"/>
                <Setter Property="Width" Value="72"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="IsThumbToolTipEnabled" Value="False"/>
                <Setter Property="Foreground" Value="{StaticResource SliderFillBrush}"/>
                <Setter Property="Background" Value="{StaticResource SliderTrackBrush}"/>
                <Setter Property="Template" Value="{StaticResource FlatSliderTemplate}"/>
            </Style>

            <Style x:Key="ProgressSliderStyle" TargetType="Slider">
                <Setter Property="Height" Value="16"/>
                <Setter Property="MinHeight" Value="16"/>
                <Setter Property="Width" Value="Auto"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="IsThumbToolTipEnabled" Value="False"/>
                <Setter Property="Foreground" Value="{StaticResource SliderFillBrush}"/>
                <Setter Property="Background" Value="{StaticResource SliderTrackBrush}"/>
                <Setter Property="Template" Value="{StaticResource FlatSliderTemplate}"/>
            </Style>

            <Style x:Key="PlayerSliderStyle" TargetType="Slider" BasedOn="{StaticResource ProgressSliderStyle}"/>

            <Style x:Key="CompactVolumeSliderStyle" TargetType="Slider" BasedOn="{StaticResource VolumeSliderStyle}">
                <Setter Property="Width" Value="56"/>
            </Style>

            <SolidColorBrush x:Key="ChromeBrush" Color="#700F172A"/>
            <SolidColorBrush x:Key="ControlBarBrush" Color="#7A0F172A"/>
            <SolidColorBrush x:Key="GlassHoverBrush" Color="#35FFFFFF"/>
            <SolidColorBrush x:Key="GlassPressedBrush" Color="#50FFFFFF"/>
            <SolidColorBrush x:Key="PlaylistPanelBrush" Color="#B00A0E1A"/>
            <SolidColorBrush x:Key="PlaylistPanelBorderBrush" Color="#22FFFFFF"/>
            <SolidColorBrush x:Key="GlassPanelBorderBrush" Color="#22FFFFFF"/>

            <RadialGradientBrush x:Key="EmptyStateGlowBrush"
                                 GradientOrigin="0.5,0.5"
                                 Center="0.5,0.5"
                                 RadiusX="0.55"
                                 RadiusY="0.55">
                <GradientStop Color="#806366F1" Offset="0"/>
                <GradientStop Color="#504338CA" Offset="0.5"/>
                <GradientStop Color="#004338CA" Offset="1"/>
            </RadialGradientBrush>

            <SolidColorBrush x:Key="MenuBarItemHoverBrush" Color="#30FFFFFF"/>

            <ControlTemplate x:Key="IconOnlyButtonTemplate" TargetType="controls:HandButton">
                <Border x:Name="Root"
                        Background="Transparent"
                        BorderThickness="0"
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        Padding="{TemplateBinding Padding}">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal"/>
                            <VisualState x:Name="PointerOver"/>
                            <VisualState x:Name="Pressed"/>
                            <VisualState x:Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Target="Root.Opacity" Value="0.35"/>
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      Content="{TemplateBinding Content}"
                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                      Foreground="{TemplateBinding Foreground}"/>
                </Border>
            </ControlTemplate>

            <Style x:Key="VideoOverlayButtonStyle" TargetType="controls:HandButton">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="UseSystemFocusVisuals" Value="False"/>
                <Setter Property="Template" Value="{StaticResource IconOnlyButtonTemplate}"/>
            </Style>

            <Style x:Key="TransportButtonStyle" TargetType="controls:HandButton">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Width" Value="40"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Foreground" Value="{StaticResource SubtleTextBrush}"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="UseSystemFocusVisuals" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="controls:HandButton">
                            <Border x:Name="Root"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Width="{TemplateBinding Width}"
                                    Height="{TemplateBinding Height}"
                                    Padding="{TemplateBinding Padding}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="{StaticResource GlassHoverBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="{StaticResource GlassPressedBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Opacity" Value="0.35"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="VolumeButtonStyle" TargetType="controls:HandButton" BasedOn="{StaticResource TransportButtonStyle}">
                <Setter Property="Width" Value="40"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="MinWidth" Value="40"/>
                <Setter Property="MinHeight" Value="40"/>
                <Setter Property="MaxWidth" Value="40"/>
                <Setter Property="MaxHeight" Value="40"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Padding" Value="0"/>
            </Style>

            <Style x:Key="PlayButtonStyle" TargetType="controls:HandButton" BasedOn="{StaticResource TransportButtonStyle}">
                <Setter Property="Width" Value="44"/>
                <Setter Property="Height" Value="44"/>
                <Setter Property="MinWidth" Value="44"/>
                <Setter Property="MinHeight" Value="44"/>
                <Setter Property="MaxWidth" Value="44"/>
                <Setter Property="MaxHeight" Value="44"/>
            </Style>

            <Style x:Key="SkipButtonStyle" TargetType="controls:HandButton" BasedOn="{StaticResource TransportButtonStyle}">
                <Setter Property="Width" Value="44"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Foreground" Value="{StaticResource SubtleTextBrush}"/>
            </Style>

            <Style x:Key="PlaylistAddButtonStyle" TargetType="controls:HandButton" BasedOn="{StaticResource TransportButtonStyle}">
                <Setter Property="Width" Value="48"/>
                <Setter Property="Height" Value="48"/>
                <Setter Property="FontSize" Value="22"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="0,0,0,2"/>
            </Style>

            <Style x:Key="PlaybackRateSelectorStyle" TargetType="controls:PlaybackRateSelector" BasedOn="{StaticResource TransportButtonStyle}">
                <Setter Property="Width" Value="56"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="MinWidth" Value="56"/>
                <Setter Property="MaxWidth" Value="56"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>

            <Style x:Key="PlaylistItemButtonStyle" TargetType="controls:HandButton">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="{StaticResource GlassPanelBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Foreground" Value="{StaticResource SubtleTextBrush}"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="VerticalAlignment" Value="Stretch"/>
                <Setter Property="UseSystemFocusVisuals" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="controls:HandButton">
                            <Border x:Name="Root"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Padding="{TemplateBinding Padding}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="{StaticResource GlassHoverBrush}"/>
                                                <Setter Target="Root.BorderBrush" Value="#55FFFFFF"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="{StaticResource GlassPressedBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PlaybackRateComboBoxStyle" TargetType="ComboBox">
                <Setter Property="Background" Value="#22FFFFFF"/>
                <Setter Property="Foreground" Value="{StaticResource SubtleTextBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="MinHeight" Value="40"/>
                <Setter Property="MaxHeight" Value="40"/>
                <Setter Property="Width" Value="76"/>
                <Setter Property="Padding" Value="0,0,22,0"/>
                <Setter Property="Margin" Value="0,0,0,1"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
